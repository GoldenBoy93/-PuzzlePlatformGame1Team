using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameUI : BaseUI
{
    // 각 종 UI 요소 추가 (체력,기력,공복 등등)

    public override void Init(UIManager uiManager)
    {
        base.Init(uiManager);
    }
}
